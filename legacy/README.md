# legacy

Notebooks from before `deepagents` 0.6 shipped (May 2026). Hand-rolled mocks to validate output shape and step ordering when the harness didn't exist yet. Kept for history.

For the current implementation see the repo root.
